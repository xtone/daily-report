- breadcrumb :project_members, @project
- content_for :title, t('.title', name: @project.name)
- content_for :head do
  = javascript_include_tag 'project_members'

h1 プロジェクトメンバー設定

p クリックすると参加状態のOn/Offを切り替えることができます。

#project_members[data-project-members-path=project_members_path(@project, format: :json)]
